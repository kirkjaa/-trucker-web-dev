/**
 * Firebase is currently disabled for the demo deployment.
 * This helper intentionally does nothing so the rest of the codebase
 * can continue to import from this module without runtime errors.
 */
export async function getMessagingInstance() {
  return null;
}
