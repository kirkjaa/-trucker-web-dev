export enum ERfqType {
  ONEWAY = "ONEWAY",
  MULTIPLE = "MULTIPLE",
  ABROAD = "ABROAD",
}

export enum ERfqSendTo {
  BOTH = "BOTH",
  DIRECT = "DIRECT",
}

export enum ERfqAbroadType {
  AIR = "AIR",
  SHIP = "SHIP",
}
